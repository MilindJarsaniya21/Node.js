// In this video we created packages.
// Package.json keeps the technical detail of the project.
// package-lock.json keeps the whole detail of the packages

// Let's use color package
const colors = require('colors');
console.log("Package.json".bgGreen);